<ContentPage xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:category="using:CatchLightning.features.Dashboard.Achievement"
             xmlns:model="using:CatchLightning.features.Dashboard.Achievement.Model"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="CatchLightning.features.Dashboard.Achievement.CategoryPage"
             x:DataType="category:CategoryPageViewModel">
  
    <ContentPage.DataTemplates>

        <DataTemplate DataType="model:CategoryViewModel">
            
            <TextBlock Text="{Binding Title}" TextTrimming="WordEllipsis"/>
        </DataTemplate>

        <DataTemplate DataType="model:GoalViewModel">

            <Button Classes="goal-tile"
                    Width="160" Height="100"
                    Margin="5" Padding="10"
                    Command="{Binding Command}">
                
                <StackPanel>
                    <TextBlock Text="{Binding Title}"/>
                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap"
                               Margin="0 10 0 10" TextTrimming="WordEllipsis"
                               Height="65"/>
                </StackPanel>
            </Button>
        </DataTemplate>
    </ContentPage.DataTemplates>

    <Grid RowDefinitions="Auto, *">

        <StackPanel Grid.Row="0" Orientation="Horizontal"  Height="40" Margin="0 12">

            <ComboBox ItemsSource="{Binding Categories}" Width="200" Margin="12"
                      SelectedItem="{Binding SelectedCategory}" VerticalAlignment="Stretch"/>

            <Button Classes="goal-tile" Margin="12"
                    VerticalAlignment="Stretch"
                    Content="Создать"
                    Command="{Binding CreateCategoryCommand}"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1">
            <ItemsControl ItemsSource="{Binding Goals}" Margin="12">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</ContentPage>
